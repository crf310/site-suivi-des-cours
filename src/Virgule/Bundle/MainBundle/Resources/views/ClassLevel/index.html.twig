<h1>ClassLevel list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Label</th>
            <th>Htmlcolorcode</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('classlevel_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.label }}</td>
            <td>{{ entity.htmlColorCode }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('classlevel_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('classlevel_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('classlevel_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
