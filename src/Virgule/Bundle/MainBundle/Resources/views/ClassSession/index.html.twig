{% extends '::main_content.html.twig' %}
{% block content_title %}Tous les comptes-rendus{% endblock %}
 {%block submenu %}
    <a class="btn tip-bottom" href="#"> <i class="icon-rss"></i>S'abonner au flux RSS</a>
    <a class="btn tip-bottom" href="{{ path('classsession_new') }}"> <i class="icon-add"></i>Nouveau compte-rendu</a>
{% endblock %}
{% block main_content %}
<div class="widget-box">
    <div class="widget-title">
            <span class="icon">
                    <i class="icon-th"></i>
            </span>
            <h5>Tous les comptes-rendus</h5>
    </div>
    <div class="widget-content nopadding">
        <table class="table table-bordered data-table">
            <thead>
                <tr>
                    <th>Date</th>
                    <th>Niveau</th>
                    <th>Cours</th>
                    <th>Apprenants présents</th>
                    <th>Dirigé par</th>
                    <th>Saisi par</th>
                    <th>Saisi le</th>
                </tr>
            </thead>
            <tbody>
            {% for entity in entities %}
                <tr>
                    <td class="center"><a href="{{ path('classsession_show', { 'id': entity['id'] }) }}">{% if entity['sessionDate'] %}{{ entity['sessionDate'] | date('d/m/Y') }} {% endif %}</a></td>
                    <td class="center"><span class="badge" style="background-color: {{ entity['classLevelHtmlColorCode'] }};">{{ entity['classLevelLabel'] }}</span></td>
                    <td>le {{ entity['course_dayOfWeek'] | day }} de {{ entity['course_startTime'] | date('H:i') }} à {{ entity['course_endTime']| date('H:i') }}</td>
                    <td class="center">{{ entity['nb_students'] }}
                    <td>{{ entity['sessionTeacher_firstName'] }} {{ entity['sessionTeacher_lastName'] }}</td>
                    <td>{{ entity['reportTeacher_firstName'] }} {{ entity['reportTeacher_lastName'] }}</td>
                    <td class="center">{% if entity['reportDate'] %}{{ entity['reportDate'] | date('d/m/Y h:i') }} {% endif %}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}